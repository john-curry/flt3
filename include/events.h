#ifndef EVENTS_H
#define EVENTS_H
namespace events {
    const event controller_confirm("confirm", "controller");
    const event controller_exit("exit", "controller");
    const event controller_up("up", "controller");
    const event controller_down("down", "controller");
    const event controller_right("right", "controller");
    const event controller_left("left", "controller");
    const event state_init("init", "state");
}
#endif
